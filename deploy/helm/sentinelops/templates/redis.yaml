apiVersion: apps/v1
kind: Deployment
metadata:
  name: sentinelops-redis
  namespace: {{ .Values.namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sentinelops-redis
  template:
    metadata:
      labels:
        app: sentinelops-redis
    spec:
      containers:
        - name: redis
          image: {{ .Values.redis.image }}
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: sentinelops-redis
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: sentinelops-redis
  ports:
    - port: 6379
      targetPort: 6379
